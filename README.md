# Git Repository Dumper

Утилита для конвертации Git-репозитория в единый текстовый файл. Идеально подходит для:
- Загрузки кода в LLM (ChatGPT, Claude, Gemini и др.)
- Анализа проектов в текстовом виде
- Создания резервных копий структуры кода
- Документирования проектов

## 📋 Особенности

- **Два режима работы**: интерактивный и быстрый (с ключом `-q`)
- **Гибкая фильтрация**: пропускает бинарные файлы, служебные папки
- **Сохраняет структуру**: относительные пути, размеры файлов, информация о Git
- **Простой интерфейс**: подсказки, примеры путей, прогресс выполнения
- **Кроссплатформенность**: работает на Linux, macOS, Windows

## 🚀 Установка

1. **Скачайте скрипт**:
```bash
wget https://raw.githubusercontent.com/KhokhlovArtem/repo-to-onefile/repo_dumper.py
```

2. **Сделайте исполняемым** (опционально):
```bash
chmod +x repo_dumper.py
```

3. **Зависимости**:
   - Python 3.6+
   - Git (для получения информации о репозитории)

## 💻 Использование

### Базовое использование

```bash
# Интерактивный режим (рекомендуется для новых пользователей)
python3 repo_dumper.py

# Быстрый режим (требует путь к репозиторию)
python3 repo_dumper.py -q

# Быстрый режим с указанием пути
python3 repo_dumper.py -q ./my-project
python3 repo_dumper.py -q /полный/путь/к/проекту
```

### Пример сессии

**Интерактивный режим**:
```
============================================================
КОНВЕРТЕР РЕПОЗИТОРИЯ В ЕДИНЫЙ ФАЙЛ
============================================================

Текущая директория: /home/user/projects
Примеры путей:
  - ./my-project
  - ../относительный/путь
  - или Enter для текущей директории

Введите путь к локальному репозиторию (Enter для текущей директории): ./my-app
✓ Используется путь: my-app
✓ Найден Git-репозиторий
```

**Быстрый режим**:
```
============================================================
БЫСТРЫЙ РЕЖИМ (QUICK MODE)
============================================================

Текущая директория: /home/user
Примеры путей:
  - ./my-project
  - ../относительный/путь
  - /полный/путь/к/репозиторию

Введите путь к репозиторию (обязательно): ./projects/my-app
✓ Используется путь: /home/user/projects/my-app
✓ Найден Git-репозиторий: my-app
✓ Выходной файл: my-app_dump.txt
```

## ⚙️ Функционал

### Режимы работы

| Режим | Ключ | Описание |
|-------|------|----------|
| Интерактивный | (без ключа) | Полный контроль: настройки фильтрации, выбор имени файла, подтверждения |
| Быстрый | `-q` | Минимальный интерфейс: обязательный путь, значения по умолчанию, немедленный старт |

### Фильтрация файлов

По умолчанию скрипт пропускает:

- **Бинарные файлы**: `.jpg`, `.png`, `.pdf`, `.zip`, `.exe` и др.
- **Служебные папки**: `.git`, `node_modules`, `venv`, `.venv`
- **Крупные файлы**: можно задать лимит размера (опционально)

В интерактивном режиме все настройки можно изменить.

### Формат выходного файла

```
================================================================================
ДАМП РЕПОЗИТОРИЯ: my-project
ФИЛЬТРЫ: пропускать бинарные=True, .git=True
================================================================================


============================================================
ФАЙЛ: README.md
РАЗМЕР: 1500 байт
============================================================

# Мой проект
Описание проекта...


============================================================
ФАЙЛ: src/main.py
РАЗМЕР: 800 байт
============================================================

import sys

def main():
    print("Hello World")


================================================================================
ИНФОРМАЦИЯ О GIT
================================================================================

Текущая ветка: main
Последний коммит: a1b2c3d Обновлен README
Нет несохраненных изменений
```

## 🛠️ Технические детали

### Поддерживаемые системы

- **Linux** (Ubuntu, Debian, Fedora, Arch и др.)
- **macOS** (10.15+)
- **Windows** (10/11, через Python)

### Требования к Python

- Стандартная библиотека Python 3.6+
- Дополнительные модули не требуются

### Обрабатываемые файлы

- Текстовые файлы любого расширения
- Конфигурационные файлы
- Скрипты (Python, Bash, JavaScript и др.)
- Документация (Markdown, Text, HTML)

### Игнорируемые файлы

```
# Бинарные файлы
*.jpg, *.jpeg, *.png, *.gif, *.bmp, *.ico, *.svg
*.pdf, *.zip, *.tar, *.gz, *.rar, *.7z
*.exe, *.dll, *.so, *.pyc, *.pyo, *.class, *.jar, *.war

# Служебные папки
.git/, node_modules/, venv/, .venv/, env/, .env/
```

## 🔧 Расширенное использование

### Создание псевдонима (alias)

Добавьте в `~/.bashrc` или `~/.zshrc`:

```bash
# Для Linux/macOS
alias repo2txt='python3 /путь/к/repo_dumper.py'

# Использование
repo2txt -q ./my-project
```

### Интеграция с другими инструментами

```bash
# Создать дамп и открыть в редакторе
python3 repo_dumper.py -q ./project && code project_dump.txt

# Создать дамп и отправить в LLM через curl
python3 repo_dumper.py -q ./project && \
curl https://api.openai.com/v1/chat/completions \
  -H "Authorization: Bearer $OPENAI_API_KEY" \
  -d "$(cat project_dump.txt)"
```

### Автоматизация

Создайте скрипт для регулярного создания дампов:

```bash
#!/bin/bash
# backup_repos.sh

PROJECTS=("project1" "project2" "project3")
BACKUP_DIR="/путь/к/бекапам"

for project in "${PROJECTS[@]}"; do
    echo "Backing up $project..."
    python3 repo_dumper.py -q "./$project"
    mv "${project}_dump.txt" "$BACKUP_DIR/${project}_$(date +%Y%m%d).txt"
done
```

## 📝 Примеры использования

### Пример 1: Анализ проекта для LLM

```bash
# Создаем дамп проекта
python3 repo_dumper.py -q ./machine-learning-project

# Копируем содержимое и вставляем в ChatGPT/Claude
cat machine-learning-project_dump.txt | pbcopy  # macOS
cat machine-learning-project_dump.txt | xclip -sel clip  # Linux

# Или используем напрямую
python3 -c "
import openai
content = open('machine-learning-project_dump.txt').read()
response = openai.ChatCompletion.create(
    model='gpt-4',
    messages=[{'role': 'user', 'content': f'Проанализируй этот проект:\\n{content}'}]
)
print(response.choices[0].message.content)
"
```

### Пример 2: Сравнение двух версий проекта

```bash
# Версия 1.0
git checkout v1.0
python3 repo_dumper.py -q ./project
mv project_dump.txt project_v1.0.txt

# Версия 2.0
git checkout v2.0
python3 repo_dumper.py -q ./project
mv project_dump.txt project_v2.0.txt

# Сравнение
diff -u project_v1.0.txt project_v2.0.txt | head -100
```

### Пример 3: Документирование структуры проекта

```bash
# Создаем документацию
python3 repo_dumper.py ./my-app

# Добавляем в README
echo -e "\n## Полная структура проекта\n" >> README.md
echo '```' >> README.md
head -100 my-app_dump.txt | grep "ФАЙЛ:" >> README.md
echo '```' >> README.md
```

## ❓ Часто задаваемые вопросы

### Q: Скрипт не находит .git папку
**A**: Это не ошибка. Скрипт продолжит работу, но без информации о Git. Убедитесь, что это Git-репозиторий, или продолжайте для обычной папки.

### Q: Выходной файл слишком большой
**A**: Используйте фильтрацию:
- Установите лимит размера файлов
- Исключите больше типов файлов
- Пропустите папки с зависимостями (node_modules, venv)

### Q: Как изменить настройки фильтрации?
**A**: В интерактивном режиме на вопрос "Изменить настройки фильтрации?" ответьте `y`.

### Q: Можно ли использовать для не-Git проектов?
**A**: Да, скрипт работает с любыми папками. Просто подтвердите продолжение при предупреждении об отсутствии .git.

### Q: Как изменить имя выходного файла?
**A**: В интерактивном режиме откажитесь от имени по умолчанию. В быстром режиме переименуйте файл после создания.

## 🐛 Отладка проблем

### Проверка зависимостей

```bash
# Проверка Python
python3 --version

# Проверка Git
git --version

# Проверка прав доступа
ls -la repo_dumper.py
```

### Логирование

Для отладки добавьте флаг `-v` (если реализовано) или модифицируйте скрипт:

```python
# Добавьте в начало main()
import logging
logging.basicConfig(level=logging.DEBUG)
```

### Распространенные ошибки

1. **Permission denied**
   ```bash
   chmod +x repo_dumper.py
   ```

2. **Python not found**
   ```bash
   # Используйте python вместо python3
   python repo_dumper.py
   ```

3. **Путь не существует**
   ```bash
   # Проверьте путь
   ls -la ./ваш-проект
   ```

## 📄 Лицензия

MIT License. См. файл [LICENSE](LICENSE).

## 🤝 Вклад в проект

1. Форкните репозиторий
2. Создайте ветку для фичи (`git checkout -b feature/amazing-feature`)
3. Закоммитьте изменения (`git commit -m 'Add amazing feature'`)
4. Запушьте ветку (`git push origin feature/amazing-feature`)
5. Откройте Pull Request

## 📞 Поддержка

- **Issues**: [GitHub Issues](https://github.com/KhokhlovArtem/repo-to-onefile/issues)
- **Обсуждения**: [Discussions](https://github.com/KhokhlovArtem/repo-to-onefile/discussions)

---

## 🚀 Быстрый старт (TL;DR)

```bash
# 1. Скачайте скрипт
curl -O https://raw.githubusercontent.com/KhokhlovArtem/repo-to-onefile/repo_dumper.py

# 2. Запустите на своем проекте
python3 repo_dumper.py -q ./ваш-проект

# 3. Используйте полученный файл
#    - Загрузите в LLM
#    - Сохраните как документацию
#    - Проанализируйте структуру
```

**Совет**: Для регулярного использования создайте псевдоним `alias repo2txt='python3 /путь/к/repo_dumper.py'`
